import { Meta, Canvas } from '@storybook/addon-docs'
import { ArgTypes } from '@storybook/blocks';

import { CloseButton } from '.'

import * as stories from './CloseButton.stories'

<Meta of={stories} />

# CloseButton

component for firing internal close and clean common actions

## Install

```sh
npm install @spark-ui/close-button
```

## Import

```tsx
import { CloseButton } from "@spark-ui/close-button"
```

## Props

<ArgTypes of={CloseButton} />

## Variants
<Canvas of={stories.Default} />
