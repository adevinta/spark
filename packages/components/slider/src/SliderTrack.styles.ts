import { cva, cx } from 'class-variance-authority'

export const trackStyles = cx('relative rounded-sm grow h-sm bg-on-background/dim-4')

export const rangeStyles = cva('absolute rounded-sm bg-basic h-full')
