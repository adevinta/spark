import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Source } from '@storybook/blocks'
import { StoryHeading } from '@docs/helpers/StoryHeading'

import { Button } from '.'

import * as ButtonStories from './Button.stories'

<Meta of={ButtonStories} />

# Button

Button component is used to trigger an action or event, such as submitting a form, opening a Dialog, canceling an action, or performing a delete operation.

<StoryHeading label="Install" />

```sh
npm install @spark-ui/button
```

<StoryHeading label="Import" />

```js
import { Button } from '@spark-ui/button'
```

<StoryHeading label="Props" />
<ArgsTable of={Button} />

<StoryHeading label="Default" />

<Canvas>
  <Story of={ButtonStories.Default} />
</Canvas>

<StoryHeading label="Disabled" />

Use `disable` prop to disable the element.

<Canvas>
  <Story of={ButtonStories.Disabled} />
</Canvas>

<StoryHeading label="Sizes" />

Use `size` prop to set the size of a button.

<Canvas>
  <Story of={ButtonStories.Sizes} />
</Canvas>

<StoryHeading label="Shapes" />

Use `shape` prop to set the shape of a button.

<Canvas>
  <Story of={ButtonStories.Shapes} />
</Canvas>

<StoryHeading label="Design" />

Use `design` prop to set the different look and feels of a button.

<Canvas>
  <Story of={ButtonStories.Design} />
</Canvas>

<StoryHeading label="Intent" />

Use `intent` prop to set the color intent of a button.

<Canvas>
  <Story of={ButtonStories.Intent} />
</Canvas>
