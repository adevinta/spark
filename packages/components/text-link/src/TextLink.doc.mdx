import { Meta, Canvas } from '@storybook/blocks'
import { A11yReport } from '@docs/helpers/A11yReport'
import { ArgTypes } from '@storybook/blocks'
import { Kbd } from '@spark-ui/kbd'
import { Callout } from '@docs/helpers/Callout'

import { TextLink } from '.'

import * as stories from './TextLink.stories'

<Meta of={stories} />

# TextLink

TextLink is an accessible element for navigation.

## Install

```sh
npm install @spark-ui/text-link
```

## Import

```tsx
import { TextLink } from '@spark-ui/text-link'
```

## Props

This component support [all HTML attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/a#attributes) supported by the `a` tag.

<ArgTypes of={TextLink} />

## Usage

### Default

<Canvas of={stories.Default} />

### Bold

By default, `TextLink` will inherit the font weight of the current font.

Use `bold` to highlight a link inside a paragraph.

<Canvas of={stories.Bold} />

### Intents

By default, `TextLink` will inherit the color of the current font.

<Canvas of={stories.CurrentIntent} />

You can also manually set the `intent` prop when you need a specific color instead.

<Canvas of={stories.Intents} />

### Sizes

By default, the `TextLink` component inherits the font-size of its parent element.

Simply use css classes to set a difference size if necessary.

<Canvas of={stories.Sizes} />

## Advanced Usage

### Icons

You can use Spark’s `Icon` component inside of the `TextLink` to display an icon.

This can be useful to showcase external links, download links, etc.

<Canvas of={stories.Icons} />

### Underline

By default `TextLink` shows an underline. It can be disabled to only be shown when hovering/focusing the link instead.

<Callout kind="warning">
  <span>
    If you decide to remove the underline, the links might become more difficult to identify by
    users.
  </span>
  <span>
    <span>Please check the </span>
    <TextLink
      href="https://www.w3.org/WAI/WCAG22/Techniques/failures/F73"
      children="official WCAG requirements"
    />
    <span> for accessible links.</span>
  </span>
</Callout>

<Canvas of={stories.Underline} />

## Accessibility

<A11yReport of="text-link" />

Adheres to the [Link WAI-ARIA design pattern](https://www.w3.org/WAI/ARIA/apg/patterns/link/).

### Keyboard Interactions

- <Kbd>Enter</Kbd> - Opens the link
- <Kbd>Tab</Kbd> - Moves focus to the next focusable element.
- <Kbd>Shift</Kbd> + <Kbd>Tab</Kbd> - Moves focus to the previous focusable element.
