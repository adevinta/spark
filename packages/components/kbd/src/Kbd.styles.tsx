import { cva } from 'class-variance-authority'

export const kbdStyles = cva([
  'border-sm border-b-md bg-neutral-container ps-md pe-md border-outline whitespace-nowrap rounded-sm font-mono text-[0.8em] font-bold leading-none',
])
